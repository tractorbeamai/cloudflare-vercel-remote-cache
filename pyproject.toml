[project]
name = "turbo-cache-contract-tests"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ["schemathesis", "pytest", "requests"]
[tool.pytest.ini_options]
testpaths = ["tests/contract"]

[dependency-groups]
dev = [
    "ruff>=0.16.8",
]
